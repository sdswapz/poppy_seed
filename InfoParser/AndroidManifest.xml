<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="me.swapnil.infoparser"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk android:minSdkVersion="14"/>
    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher">
    <activity android:name="AppListActivity"
              android:label="@string/app_name">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
    </activity>
    <activity android:name=".AppDetailActivity" android:label="Detailed List"></activity>
    <activity android:name=".MoreDetailedActivity" android:label="Complete List"></activity>
    <activity android:name=".SubDetailActivity" android:label="Info Activity"></activity>
    </application>
</manifest>
